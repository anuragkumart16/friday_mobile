name: friday_mobile
description: "mobile app to talk with friday"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  flutter_markdown: ^0.7.6
  file_picker: ^8.0.0
  share_plus: ^10.0.0
  flutter_tts: ^4.2.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

